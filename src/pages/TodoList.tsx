import AppLayout from "../layouts/AppLayout";

const TodoList = () => {
  return (
    <div className="bg-app h-screen">
      <AppLayout />
      <div>todo</div>
    </div>
  );
};

export default TodoList;
